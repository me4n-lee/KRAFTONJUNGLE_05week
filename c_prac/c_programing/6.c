// Q 6 - The types of linkages are,

// A : Internal linkage, External linkage and None linkage

// External Linkage-> 전역(global)이면서 정적(static)이 아닌 변수와 함수
// Internal Linkage-> 파일 범위(file scope)를 가지는 정적(static) 변수와 함수
// None Linkage-> 지역 변수(local variables)