// Q 8 - return 키워드는 함수로부터 제어를 호출한 함수로 다시 전달하는 데 사용됩니다.

// 정답 : yes

// C 언어에서, return 함수는 함수의 실행을 중지하고 값을 가지고 호출한 함수로 제어를 돌려줍니다. 
// 호출 함수에서 실행은 호출 바로 다음부터 시작됩니다.

// 즉, C 언어에서 return 문은 현재 실행 중인 함수의 작업을 중단하고, 
// 값을 반환한 다음 호출한 함수로 제어를 전달합니다.
// 호출한 함수에서는 호출된 지점 바로 다음부터 실행이 계속됩니다.