// Q8 : Which of the following is true

// A : gets() can read a string with spaces 
// but a normal scanf() with %s can not.

// C 언어에서, gets() 함수는 공백 문자를 포함한 전체 줄을 읽을 수 있지만, 
// scanf() 함수에서 %s 서식 지정자를 사용하여 문자열을 입력받을 때는 
// 공백 문자 이전까지의 문자만 읽습니다. 
// 따라서 gets() 함수를 사용하면 공백 문자를 포함한 문자열을 읽을 수 있지만, 
// scanf() 함수를 사용할 때는 공백 문자가 나오면 문자열의 입력이 중단됩니다.